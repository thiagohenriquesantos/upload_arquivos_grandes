<p id="notice"><%= notice %></p>

<h1>Arquivos</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Processado</th>
      <th>Url arquivo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @arquivos.each do |arquivo| %>
      <tr>
        <td><%= arquivo.nome %></td>
        <td><%= arquivo.processado %></td>
        <td><%= arquivo.url_arquivo %></td>
        <td><%= link_to 'Show', arquivo %></td>
        <td><%= link_to 'Edit', edit_arquivo_path(arquivo) %></td>
        <td><%= link_to 'Destroy', arquivo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Arquivo', new_arquivo_path %>
